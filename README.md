# tree-sitter-swayconfig

🌳 Tree-sitter parser for Sway window manager configuration files. LLM Generated

## About

This is a Tree-sitter grammar for parsing Sway configuration files. It supports all major Sway configuration directives including:

- Variable definitions (`set`)
- Key bindings (`bindsym`, `bindcode`, `bindgesture`, `bindswitch`)
- Execution commands (`exec`, `exec_always`)
- Output configuration
- Input device configuration
- Seat configuration
- Workspace configuration
- Mode blocks
- Bar configuration blocks
- Window criteria and assignments
- Floating window configuration
- Gaps configuration
- Focus behavior
- Client colors
